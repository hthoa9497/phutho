{#
/**
 * @file
 * Default theme implementation to present an author management entity.
 *
 * This template is used when viewing a registered author management's page,
 * e.g., /admin/content/author-management)/123. 123 being the author management's ID.
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 *   print a subset such as 'content.title'.
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_author_management()
 */
#}
<article{{ attributes }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</article>
